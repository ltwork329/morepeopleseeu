# progress

## 2026-04-26
- 新增“批量制作”页面：可按批次选择声音、语言、确认字幕预览、套用标题/字幕样式，并一键批量合成。
- 新增批量导出到桌面自命名文件夹：后端会创建导出目录并把成片复制过去。
- 新增一键生成并打开文件夹：自动打开素材放置目录 `local_materials/unused` 和桌面成片导出目录。
- 批量合成新增唯一素材前置检查：同一批次按素材源文件去重，唯一素材数量不足时直接停止，不做半成品。
- 页面视觉统一为年轻粉色系：背景、侧栏、卡片、按钮和状态区改为青春明亮风格。
- 已新增一键命令 `npm run materials:init`，自动创建三类素材文件夹并立即扫描。
- 已在输出记录中增加编号显示。
- 已将素材库单条列表改为带编号的“单个视频时长清单”。
- 已将文案上传页语言选择移除，改为在生成音频步骤选择语言。
- 已将导出记录彻底并入视频合成页，不再依赖单独分页入口。
- 已简化页面提示文案，保留必要状态，去掉长段提示。
- 已将素材库展示改为三类文件夹下每条素材时长 + 分类时长。
- 已将生成音频页改为先选择已上传文案记录，再选择声音生成音频。
- 已将视频合成页改为先选择已生成音频的记录，再进入合成。
- 已恢复视频合成页大预览框，没有任务时也显示清晰的空预览区域。
- 已将标题/字幕样式控件放在大预览框旁边，保持同页调整。
- 已将素材库改为扫描统计视图，不再要求手填素材名称、分类、秒数。
- 已新增并跑通素材扫描脚本：`npm run scan:materials`。
- 已在视频合成页增加标题/字幕默认样式说明。
- 已让声音克隆结果可复用，生成音频页可以选择已有声音。
- 已将音频生成页简化为选择声音和生成音频，不显示复杂进度。
- 已将合成进度条放到视频合成页。
- 已在视频合成页新增就近合成记录，显示已完成、处理中、待处理和当前任务状态。
- 已修复视频合成页拖动逻辑：拖动时不重绘页面，松手后保存位置。
- 已按抖音/短视频规则将标题和字幕预览改为默认单行显示，避免稍微变大就换行。
- 已新增素材文件夹初始化脚本：`scripts/setup_material_folders.ps1`。
- 已创建固定素材目录：`local_materials/unused`、`local_materials/fragments`、`local_materials/used`。
- 已确认素材不依赖文件命名，用户只需要放进对应文件夹。
- 已将文案编号改为带日期格式，例如 `20260426_0001`。
- 已从文案上传页移除标题字号和字幕字号设置。
- 已确认标题和字幕样式在视频合成页调整，不在上传页设置。
- 已在视频合成页新增标题和字幕的颜色、大小、左右位置、上下位置调整。
- 已在视频合成页新增标题和字幕拖动预览入口。
- 已根据反馈重排页面主流程：文案上传、声音克隆、生成音频、视频合成、素材库、导出记录。
- 已把“工作台”改为“任务总览”，避免和文案上传入口混淆。
- 已在文案上传页显示即将自动生成的批次号和编号规则。
- 已强调标题必须和文案一起生成任务，每个视频都有标题。
- 已新增声音克隆入口和生成音频入口。
- 已创建本地 Web 工作台骨架。
- 已实现复制粘贴导入文案。
- 已实现 Excel 上传导入，识别 `title`、`body`、`language`、`title_font_size`、`subtitle_font_size`。
- 已实现批次号和文案编号生成。
- 已实现任务状态：待处理、处理中、成功、失败。
- 已实现处理中进度条和当前步骤说明。
- 已实现素材库基础录入，并在界面强调已使用素材绝对不复用。
- 已实现导出记录表基础视图。
- 已通过 `npm run build` 验证，并启动本地服务 `http://127.0.0.1:5173`。
- 已强调硬规则：已经使用的视频素材绝对不复用。
- 已确认素材已用是终态，除非用户明确重置，否则不再参与匹配。
- 已确认完整自动化最后再做，先把单条链路每一步梳理清楚。
- 已确认预览时自动抓取准备使用的视频素材首帧。
- 已确认如果素材不是从 0 秒截取，首帧应取本次截取开始时间的画面。
- 已新增页面设计方案：`ui_design_plan.md`。
- 已确认视觉方向为可爱、青春、文艺、潮流，但操作必须清晰省心。
- 已确认本地使用第一版不需要登录和改密码。
- 已确认记录页必须清晰显示成功、失败、处理中、待处理，处理中必须显示进展。
- 下一步建议先做本地 Web 工作台骨架：文案导入、批次任务列表、状态进度、素材库、导出记录。
- 已确认 FunASR 第一版采用命令行调用，不先做本地 Python 服务。
- 已确认所有界面和功能按钮必须人性化、清晰可见、避免复杂操作。
- 已确认粤语字幕识别调用 FunASR。
- 已确认 FunASR 输出字幕后进入可编辑字幕轨道，字幕样式和位置仍由预览页控制。
- 已确认第一步文案输入只做复制粘贴和本地 Excel 上传。
- 已确认导入内容按批次号和批次内编号分开管理。
- 已确认标题、正文、标题字号、字幕字号分开保存，避免后续导出混乱。
- 已确认视频素材库存必须分为素材可用、素材残片、素材已用。
- 已确认自动剪辑默认使用音频时长 + 1 秒，并扣减素材剩余可用片段。
- 已新增当前需求真相表：`requirements_current.md`。

## 2026-04-25
- 已确认当前目录为空项目，无历史代码冲突风险。
- 已建立项目基线规范，明确“只做短视频自动化链路”。
- 已完成 skill 清单、缺口识别、建议新增 skill。
- 已完成分阶段实施路线图与验收口径。
- 已建立任务台账机制（task_plan/progress/findings）。
- 已安装短视频核心 skills：`sora`、`speech`、`transcribe`、`playwright`、`playwright-interactive`、`screenshot`。
- 已新增项目内安装脚本：`scripts/install_video_skills.sh`（可重复执行）。
- 已新增项目内落地清单：`03_剪辑必备skills安装清单.md`。
- 已补全网页生产级 skills：`chatgpt-apps`、`openai-docs`、`figma-*`、`vercel/netlify/cloudflare/render-deploy`、`sentry`。
- 已新增网页技能清单：`04_网页必备skills安装清单.md`。
- 已新增网页技能一键安装脚本：`scripts/install_web_skills.sh`。
- 已完成 MiniMax 国际版语音接入规范：`05_语音补充_minimax国际版接入规范.md`。
- 已新增 MiniMax 配置模板：`configs/tts_minimax.env.example`。
- 已新增 MiniMax 环境校验脚本：`scripts/validate_minimax_env.sh`。
- 已新增项目内 skill：`skills/sv-minimax-tts/SKILL.md`。

- 2026-04-27�����ύ������Ϊҳ���м��Զ��嵯�����˵���Ϊ������Ƶ/������Ƶ�����񿴰��Ϊ�۰׼����ʽ����������ԭ����࣬ԭ��̶����Ҳ࣬��������п�������Ƶ��
